<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.betuls.project3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TavsanActivity"></activity>
        <activity android:name=".SifrelemeActivity" />
        <activity android:name=".ZombiActivity" />
        <activity android:name=".KutuphaneActivity" />
        <activity android:name=".SacActivity" />
        <activity android:name=".TicTacActivity" />
        <activity android:name=".TitanicActivity" />
        <activity android:name=".TahminActivity" />
        <activity android:name=".DiceActivity" />
        <activity android:name=".JourneyActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>